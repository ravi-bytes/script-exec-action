import os
import subprocess
import socket
import urllib
import shutil
import ctypes
open(
eval(
exec(
os.system
sys.exit
__import__
input(
pickle
marshal
getattr(
setattr(
delattr(
globals(
locals(
compile(
breakpoint(
